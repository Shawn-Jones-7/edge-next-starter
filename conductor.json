{
  "scripts": {
    "setup": "chmod +x ./scripts/conductor-setup.sh && ./scripts/conductor-setup.sh",
    "run": "pnpm dev --port $CONDUCTOR_PORT",
    "test": "pnpm test",
    "lint": "pnpm lint"
  }
}
